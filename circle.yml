machine:
  timezone: UTC
  java:
    version: oraclejdk8

dependencies:
  cache_directories:
    - "~/.sbt"
    - "~/.ivy2"
  pre:
    - cd wvlet-ui && npm install jsdom
  override:
    - ./sbt "+ compile"

test:
  override:
    - ./sbt "+ test"
    - ./sbt coverage test coverageReport
  post:
    - ./sbt coverageAggregate coveralls
